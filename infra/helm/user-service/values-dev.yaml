# Dev environment overrides
# Only specify values that DIFFER from values.yaml

replicaCount: 1

image:
  tag: fd3c593

postgres:
  password: postgres
  
resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "256Mi"
    cpu: "200m"