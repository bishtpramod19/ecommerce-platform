apiVersion: v2
name: user-service
description: User Service for E-Commerce Platform
type: application
version: 0.2.0        # Chart version (changes when chart templates change)
appVersion: "1.0.0"   # Application version (informational only)